# 개발 지침

- 기존 `D:\golang\go-workspace\kadmin`은 읽기 전용 참조 대상이다. 소스, 설정, Git 상태를 변경하거나 서비스를 실행하지 않는다.
- Kafka와 DB에는 직접 접근하지 않는다. 모든 업무 조회는 기존 AbleOps REST API를 경유한다.
- 기존 프로젝트의 `internal` 패키지, 로컬 `replace`, `go.work`에 의존하지 않는다.
- 대상 도구는 `cluster_id`를 필수로 받으며 기본 클러스터로 대체하지 않는다. 권한 판단은 매 요청마다 백엔드가 수행한다.
- API 계약은 로컬 핸들러와 테스트로 확인하고 참조 경로, HEAD, 관련 미커밋 변경을 `docs/api-mapping.md`에 기록한다. 경로나 필터를 추측하지 않는다.
- 토큰, Authorization 헤더, 비밀번호, 인증 설정, 메시지 본문을 응답이나 로그에 노출하지 않는다. 예시는 합성 데이터만 사용한다.
- stdout은 MCP 전용이며 로그는 stderr에만 기록한다. HTTP 200 본문의 실패 상태와 부분 실패를 보존한다.
- DTO는 공개할 필드만 선언한다. 전송 크기 제한과 도구 출력 제한을 구분하고 잘림을 표시한다.
- 실제 운영 서비스 없이 `httptest`와 공식 SDK 클라이언트로 검증한다. 완료 전 `go test ./...`, `go vet ./...`, `go build ./cmd/ableops-kafka-mcp`를 실행한다.
- 문서와 코드 주석은 한국어로 작성한다. 실행 동작, 제한, 인증 경계를 바꾸면 관련 문서를 함께 수정한다.
- 원격 저장소 생성, push, 배포는 별도 사용자 요청 없이 수행하지 않는다.
